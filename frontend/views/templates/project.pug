mixin project(projectID, projectName, numTags, numFlashCards)
    .panel-clickable(data-href="/project/" + projectID)
        +panel-info(projectName)
            p Number of Tags: #{numTags}
            p Number of Flashcards: #{numFlashCards}